<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.campus.campus_life_ai.ai.mapper.AiMessageMapper">

    <resultMap id="AiMessageResultMap" type="com.campus.campus_life_ai.ai.entity.AiMessage">
        <id property="id" column="id"/>
        <result property="sessionId" column="session_id"/>
        <result property="userId" column="user_id"/>
        <result property="role" column="role"/>
        <result property="contentType" column="content_type"/>
        <result property="content" column="content"/>
        <result property="messageStatus" column="message_status"/>
        <result property="providerCode" column="provider_code"/>
        <result property="modelCode" column="model_code"/>
        <result property="promptTokens" column="prompt_tokens"/>
        <result property="completionTokens" column="completion_tokens"/>
        <result property="totalTokens" column="total_tokens"/>
        <result property="latencyMs" column="latency_ms"/>
        <result property="finishReason" column="finish_reason"/>
        <result property="errorCode" column="error_code"/>
        <result property="errorMessage" column="error_message"/>
        <result property="replyToMessageId" column="reply_to_message_id"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.campus.campus_life_ai.ai.entity.AiMessage" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_message (
            session_id, user_id, role, content_type, content, message_status, provider_code, model_code,
            prompt_tokens, completion_tokens, total_tokens, latency_ms, finish_reason, error_code, error_message,
            reply_to_message_id, created_at
        ) VALUES (
            #{sessionId}, #{userId}, #{role}, #{contentType}, #{content}, #{messageStatus}, #{providerCode}, #{modelCode},
            #{promptTokens}, #{completionTokens}, #{totalTokens}, #{latencyMs}, #{finishReason}, #{errorCode}, #{errorMessage},
            #{replyToMessageId}, #{createdAt}
        )
    </insert>

    <select id="findBySessionId" resultMap="AiMessageResultMap">
        SELECT * FROM ai_message
        WHERE session_id = #{sessionId}
        ORDER BY created_at ASC, id ASC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="findRecentContext" resultMap="AiMessageResultMap">
        SELECT * FROM (
            SELECT *
            FROM ai_message
            WHERE session_id = #{sessionId}
            ORDER BY created_at DESC, id DESC
            LIMIT #{limit}
        ) t
        ORDER BY t.created_at ASC, t.id ASC
    </select>

    <select id="countBySessionId" resultType="int">
        SELECT COUNT(*) FROM ai_message WHERE session_id = #{sessionId}
    </select>

</mapper>
