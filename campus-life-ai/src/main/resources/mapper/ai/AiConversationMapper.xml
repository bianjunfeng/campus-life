<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.campus.campus_life_ai.ai.mapper.AiConversationMapper">

    <resultMap id="AiConversationResultMap" type="com.campus.campus_life_ai.ai.entity.AiConversation">
        <id property="id" column="id"/>
        <result property="sessionId" column="session_id"/>
        <result property="userId" column="user_id"/>
        <result property="assistantType" column="assistant_type"/>
        <result property="capabilityCode" column="capability_code"/>
        <result property="sceneCode" column="scene_code"/>
        <result property="providerCode" column="provider_code"/>
        <result property="modelCode" column="model_code"/>
        <result property="title" column="title"/>
        <result property="status" column="status"/>
        <result property="lastMessagePreview" column="last_message_preview"/>
        <result property="lastMessageAt" column="last_message_at"/>
        <result property="messageCount" column="message_count"/>
        <result property="pinned" column="pinned"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.campus.campus_life_ai.ai.entity.AiConversation" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_conversation (
            session_id, user_id, assistant_type, capability_code, scene_code, provider_code, model_code,
            title, status, last_message_preview, last_message_at,
            message_count, pinned, created_at, updated_at
        ) VALUES (
            #{sessionId}, #{userId}, #{assistantType}, #{capabilityCode}, #{sceneCode}, #{providerCode}, #{modelCode},
            #{title}, #{status}, #{lastMessagePreview}, #{lastMessageAt},
            #{messageCount}, #{pinned}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <select id="findBySessionId" resultMap="AiConversationResultMap">
        SELECT * FROM ai_conversation WHERE session_id = #{sessionId} LIMIT 1
    </select>

    <select id="findBySessionIdAndUserId" resultMap="AiConversationResultMap">
        SELECT * FROM ai_conversation
        WHERE session_id = #{sessionId}
          AND user_id = #{userId}
          AND status != 2
        LIMIT 1
    </select>

    <select id="findByUserId" resultMap="AiConversationResultMap">
        SELECT * FROM ai_conversation
        WHERE user_id = #{userId}
          AND status != 2
        ORDER BY COALESCE(last_message_at, created_at) DESC, id DESC
    </select>

    <update id="updateTitle">
        UPDATE ai_conversation
        SET title = #{title},
            updated_at = NOW()
        WHERE session_id = #{sessionId}
          AND user_id = #{userId}
          AND status != 2
    </update>

    <update id="updateMessageState" parameterType="com.campus.campus_life_ai.ai.entity.AiConversation">
        UPDATE ai_conversation
        SET assistant_type = #{assistantType},
            capability_code = #{capabilityCode},
            scene_code = #{sceneCode},
            provider_code = #{providerCode},
            model_code = #{modelCode},
            title = #{title},
            last_message_preview = #{lastMessagePreview},
            last_message_at = #{lastMessageAt},
            message_count = #{messageCount},
            updated_at = #{updatedAt}
        WHERE session_id = #{sessionId}
          AND user_id = #{userId}
          AND status != 2
    </update>

    <update id="markDeleted">
        UPDATE ai_conversation
        SET status = 2,
            updated_at = NOW()
        WHERE session_id = #{sessionId}
          AND user_id = #{userId}
          AND status != 2
    </update>

</mapper>
