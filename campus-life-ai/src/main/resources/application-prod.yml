spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://8.130.80.46:3306/campus_life_ai?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8}
    username: ${SPRING_DATASOURCE_USERNAME:campus_life_ai_user}
    password: ${SPRING_DATASOURCE_PASSWORD}

management:
  endpoint:
    health:
      show-details: never

jwt:
  secret: ${JWT_SECRET}

ai:
  openai-compatible:
    api-key: ${AI_OPENAI_COMPATIBLE_API_KEY}
