# 已迁移至 deploy/middleware/docker-compose.yml
# 本地仅启动中间件：
#   docker compose -f deploy/middleware/docker-compose.yml up -d
# 全栈部署见 deploy/docker/README.md

include:
  - path: middleware/docker-compose.yml
