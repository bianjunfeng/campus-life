upstream campus_life_gateway {
    server 127.0.0.1:8090;
}

server {
    listen 80;
    server_name demo.example.com;
    root /opt/campus-life/frontend/consumer-web;
    index index.html;

    client_max_body_size 20m;

    location / {
        try_files $uri $uri/ /apps/consumer-web/index.html;
    }

    location /api/ {
        proxy_pass http://campus_life_gateway;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /uploads/ {
        proxy_pass http://campus_life_gateway;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name merchant.demo.example.com;
    root /opt/campus-life/frontend/merchant-web;
    index index.html;

    client_max_body_size 20m;

    location / {
        try_files $uri $uri/ /apps/merchant-web/index.html;
    }

    location /api/ {
        proxy_pass http://campus_life_gateway;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /uploads/ {
        proxy_pass http://campus_life_gateway;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name admin.demo.example.com;
    root /opt/campus-life/frontend/admin-web;
    index index.html;

    client_max_body_size 20m;

    location / {
        try_files $uri $uri/ /apps/admin-web/index.html;
    }

    location /api/ {
        proxy_pass http://campus_life_gateway;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /uploads/ {
        proxy_pass http://campus_life_gateway;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
