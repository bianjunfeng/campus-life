spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.elasticsearch.ElasticsearchClientAutoConfiguration
      - org.springframework.boot.autoconfigure.elasticsearch.ElasticsearchRestClientAutoConfiguration
      - org.springframework.boot.autoconfigure.data.elasticsearch.ElasticsearchDataAutoConfiguration
      - org.springframework.boot.autoconfigure.data.elasticsearch.ElasticsearchRepositoriesAutoConfiguration
  datasource:
    url: ${TEST_DB_URL:jdbc:mysql://8.130.80.46:3306/campus_life?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8}
    username: ${TEST_DB_USERNAME:campus_user}
    password: ${TEST_DB_PASSWORD:020907BJf}
    driver-class-name: com.mysql.cj.jdbc.Driver
  rabbitmq:
    listener:
      simple:
        auto-startup: false
  kafka:
    listener:
      auto-startup: false
  data:
    elasticsearch:
      repositories:
        enabled: false

event:
  reliability:
    schema-auto-init: false
  monitoring:
    enabled: false
  outbox:
    enabled: false

csp:
  sentinel:
    log:
      dir: /tmp/sentinel-logs
