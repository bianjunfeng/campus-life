<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.campus.campus_life_backend.modules.merchant.mapper.MerchantTypeMapper">

    <resultMap id="MerchantTypeResultMap" type="com.campus.campus_life_backend.modules.merchant.entity.MerchantType">
        <id property="id" column="id"/>
        <result property="code" column="code"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <insert id="insertMerchantType" parameterType="com.campus.campus_life_backend.modules.merchant.entity.MerchantType"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `merchant_type` (
            code, name, description, sort_order, status, create_time, update_time
        ) VALUES (
            #{code}, #{name}, #{description}, #{sortOrder}, #{status}, NOW(), NOW()
        )
    </insert>

    <select id="findById" resultMap="MerchantTypeResultMap">
        SELECT * FROM `merchant_type` WHERE id = #{id}
    </select>

    <select id="findByCode" resultMap="MerchantTypeResultMap">
        SELECT * FROM `merchant_type` WHERE code = #{code}
    </select>

    <select id="findAllActive" resultMap="MerchantTypeResultMap">
        SELECT * FROM `merchant_type`
        WHERE status = 1
        ORDER BY sort_order DESC, id ASC
    </select>

    <update id="updateMerchantType" parameterType="com.campus.campus_life_backend.modules.merchant.entity.MerchantType">
        UPDATE `merchant_type`
        SET
            code = #{code},
            name = #{name},
            description = #{description},
            sort_order = #{sortOrder},
            status = #{status},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE `merchant_type`
        SET status = #{status}, update_time = NOW()
        WHERE id = #{id}
    </update>

</mapper>

