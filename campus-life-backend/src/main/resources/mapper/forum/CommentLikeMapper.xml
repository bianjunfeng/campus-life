<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.campus.campus_life_backend.modules.forum.mapper.CommentLikeMapper">

    <insert id="insertCommentLike" parameterType="com.campus.campus_life_backend.modules.forum.entity.CommentLike">
        INSERT INTO `comment_like` (comment_id, user_id, create_time)
        VALUES (#{commentId}, #{userId}, NOW())
    </insert>

    <select id="findByCommentIdAndUserId" resultType="com.campus.campus_life_backend.modules.forum.entity.CommentLike">
        SELECT * FROM `comment_like`
        WHERE comment_id = #{commentId} AND user_id = #{userId}
    </select>

    <select id="findLikedCommentIdsByUserIdAndCommentIds" resultType="long">
        SELECT comment_id
        FROM `comment_like`
        WHERE user_id = #{userId}
          AND comment_id IN
          <foreach collection="commentIds" item="commentId" open="(" separator="," close=")">
              #{commentId}
          </foreach>
    </select>

    <delete id="deleteCommentLike">
        DELETE FROM `comment_like`
        WHERE comment_id = #{commentId} AND user_id = #{userId}
    </delete>

</mapper>
