<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.campus.campus_life_backend.modules.forum.mapper.PostFavoriteMapper">

    <insert id="insertPostFavorite" parameterType="com.campus.campus_life_backend.modules.forum.entity.PostFavorite">
        INSERT INTO `post_favorite` (post_id, user_id, create_time)
        VALUES (#{postId}, #{userId}, NOW())
    </insert>

    <select id="findByPostIdAndUserId" resultType="com.campus.campus_life_backend.modules.forum.entity.PostFavorite">
        SELECT * FROM `post_favorite`
        WHERE post_id = #{postId} AND user_id = #{userId}
    </select>

    <select id="findByUserId" resultType="com.campus.campus_life_backend.modules.forum.entity.PostFavorite">
        SELECT * FROM `post_favorite`
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <select id="findPostIdsByUserIdWithPagination" resultType="long">
        SELECT post_id
        FROM `post_favorite`
        WHERE user_id = #{userId}
        ORDER BY create_time DESC, id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="findFavoritedPostIdsByUserIdAndPostIds" resultType="long">
        SELECT post_id
        FROM `post_favorite`
        WHERE user_id = #{userId}
          AND post_id IN
          <foreach collection="postIds" item="postId" open="(" separator="," close=")">
              #{postId}
          </foreach>
    </select>

    <delete id="deletePostFavorite">
        DELETE FROM `post_favorite`
        WHERE post_id = #{postId} AND user_id = #{userId}
    </delete>

</mapper>

