<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.campus_life_backend.modules.admin.mapper.AdminOperationLogMapper">

    <resultMap id="BaseResultMap" type="com.campus.campus_life_backend.modules.admin.entity.AdminOperationLog">
        <id column="id" property="id"/>
        <result column="admin_id" property="adminId"/>
        <result column="operation_type" property="operationType"/>
        <result column="target_type" property="targetType"/>
        <result column="target_id" property="targetId"/>
        <result column="action" property="action"/>
        <result column="old_status" property="oldStatus"/>
        <result column="new_status" property="newStatus"/>
        <result column="reason" property="reason"/>
        <result column="ip_address" property="ipAddress"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.campus.campus_life_backend.modules.admin.entity.AdminOperationLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO admin_operation_log (
            admin_id, operation_type, target_type, target_id, action,
            old_status, new_status, reason, ip_address, create_time
        ) VALUES (
            #{adminId}, #{operationType}, #{targetType}, #{targetId}, #{action},
            #{oldStatus}, #{newStatus}, #{reason}, #{ipAddress}, NOW()
        )
    </insert>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM admin_operation_log WHERE id = #{id}
    </select>

    <select id="findList" resultMap="BaseResultMap">
        SELECT * FROM admin_operation_log
        <where>
            <if test="adminId != null">
                AND admin_id = #{adminId}
            </if>
            <if test="operationType != null and operationType != ''">
                AND operation_type = #{operationType}
            </if>
            <if test="targetType != null and targetType != ''">
                AND target_type = #{targetType}
            </if>
            <if test="action != null and action != ''">
                AND action = #{action}
            </if>
            <if test="startTime != null and startTime != ''">
                AND create_time >= #{startTime}
            </if>
            <if test="endTime != null and endTime != ''">
                AND create_time &lt;= #{endTime}
            </if>
        </where>
        ORDER BY create_time DESC
        <if test="offset != null and limit != null">
            LIMIT #{offset}, #{limit}
        </if>
    </select>

    <select id="count" resultType="java.lang.Long">
        SELECT COUNT(*) FROM admin_operation_log
        <where>
            <if test="adminId != null">
                AND admin_id = #{adminId}
            </if>
            <if test="operationType != null and operationType != ''">
                AND operation_type = #{operationType}
            </if>
            <if test="targetType != null and targetType != ''">
                AND target_type = #{targetType}
            </if>
            <if test="action != null and action != ''">
                AND action = #{action}
            </if>
            <if test="startTime != null and startTime != ''">
                AND create_time >= #{startTime}
            </if>
            <if test="endTime != null and endTime != ''">
                AND create_time &lt;= #{endTime}
            </if>
        </where>
    </select>

    <select id="selectByConditions" resultMap="BaseResultMap">
        SELECT * FROM admin_operation_log
        <where>
            <if test="adminId != null">
                AND admin_id = #{adminId}
            </if>
            <if test="operationType != null and operationType != ''">
                AND operation_type = #{operationType}
            </if>
            <if test="targetType != null and targetType != ''">
                AND target_type = #{targetType}
            </if>
            <if test="action != null and action != ''">
                AND action = #{action}
            </if>
            <if test="startTime != null and startTime != ''">
                AND create_time &gt;= #{startTime}
            </if>
            <if test="endTime != null and endTime != ''">
                AND create_time &lt;= #{endTime}
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByConditions" resultType="java.lang.Long">
        SELECT COUNT(*) FROM admin_operation_log
        <where>
            <if test="adminId != null">
                AND admin_id = #{adminId}
            </if>
            <if test="operationType != null and operationType != ''">
                AND operation_type = #{operationType}
            </if>
            <if test="targetType != null and targetType != ''">
                AND target_type = #{targetType}
            </if>
            <if test="action != null and action != ''">
                AND action = #{action}
            </if>
            <if test="startTime != null and startTime != ''">
                AND create_time &gt;= #{startTime}
            </if>
            <if test="endTime != null and endTime != ''">
                AND create_time &lt;= #{endTime}
            </if>
        </where>
    </select>

    <select id="countDistinctIpBetween" resultType="java.lang.Long">
        SELECT COUNT(DISTINCT ip_address)
        FROM admin_operation_log
        WHERE ip_address IS NOT NULL
          AND ip_address != ''
          AND create_time >= #{startTime}
          AND create_time &lt;= #{endTime}
    </select>

</mapper>

